---
phase: 05-dashboard
plan: 04
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - frontend/e2e/auth.spec.ts
  - frontend/e2e/dashboard.spec.ts
  - frontend/e2e/signals.spec.ts
  - frontend/e2e/portfolio.spec.ts
  - frontend/e2e/backtest.spec.ts
  - frontend/playwright.config.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
must_haves:
  truths:
    - "npx playwright test runs without test runner errors (even if some tests fail on empty data)"
    - "Auth spec: login with valid creds succeeds, wrong creds shows error, logout redirects to /login"
    - "Protected route spec: /dashboard without auth redirects to /login"
    - "Dashboard spec: all 4 tabs render without JavaScript errors"
    - "Signals spec: WebSocket mock delivers signal_update and signal card appears in DOM"
    - "Portfolio spec: portfolio view mounts without crash"
    - "Backtest spec: Run button triggers POST /api/backtest and renders metrics when result arrives"
    - "Mobile responsive spec: viewport 390x844 (iPhone 14) — no horizontal scroll, tabs usable"
  artifacts:
    - path: "frontend/e2e/auth.spec.ts"
      provides: "Login, logout, wrong-password, protected-route redirect tests"
    - path: "frontend/e2e/dashboard.spec.ts"
      provides: "Tab navigation, WebSocket mock, candlestick chart render, real-time update tests"
    - path: "frontend/e2e/signals.spec.ts"
      provides: "Signal feed render, WebSocket signal_update injection test"
    - path: "frontend/e2e/portfolio.spec.ts"
      provides: "Portfolio view render, positions table, charts present"
    - path: "frontend/e2e/backtest.spec.ts"
      provides: "Backtest form submit, metrics render, equity curve present"
  key_links:
    - from: "frontend/e2e/auth.spec.ts"
      to: "frontend/src/pages/Login.tsx"
      via: "page.fill + page.click on login form"
    - from: "frontend/e2e/dashboard.spec.ts"
      to: "frontend/src/hooks/useWebSocket.ts"
      via: "page.routeWebSocket('/ws/live', ...) mock"
    - from: "frontend/e2e/signals.spec.ts"
      to: "frontend/src/components/signals/SignalFeed.tsx"
      via: "page.getByTestId('signal-aapl')"
---

<objective>
Write comprehensive Playwright E2E tests covering every user interaction: auth flows, tab navigation, WebSocket real-time updates, chart rendering, portfolio display, backtest execution, and mobile responsiveness. Use page.routeWebSocket() to mock /ws/live. Mock backend REST via page.route() so tests run without a live backend.

Purpose: Automated regression gate — changes that break visible user flows fail CI before they reach the user.
Output: 5 Playwright spec files with full coverage. npx playwright test completes without runner errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-03-SUMMARY.md

<interfaces>
<!-- Test IDs from Plan 03 components -->
data-testid values guaranteed in components:
  signal-{symbol.toLowerCase()}   — each signal card (e.g. signal-aapl)
  portfolio-view                  — PortfolioView root
  positions-table                 — positions shadcn Table
  equity-curve-chart              — EquityCurveChart ResponsiveContainer
  allocation-pie-chart            — PnLPieChart ResponsiveContainer
  backtest-results                — BacktestResults root
  backtest-metrics                — metrics grid
  backtest-equity-chart           — equity curve in backtest
  dashboard-nav                   — top nav bar
  dashboard-tabs                  — shadcn Tabs root

<!-- Mock data shapes for page.route() -->
Signals mock:
[{ "symbol": "AAPL", "direction": "BUY", "confidence": 85, "regime": "trending",
   "close": 171.8, "entry_price": 172.0, "stop_loss": 168.0, "target_price": 180.0,
   "rsi_14": 55.2, "macd_val": 1.34, "adx_14": 28.5, "atr_14": 2.1, "reasons": ["RSI bullish"],
   "interval": "1D", "scanned_at": "2026-04-08T12:00:00" }]

Candles mock (DESC order — as backend returns, component will reverse):
[{ "time": 1714608000, "open": 172, "high": 173, "low": 171, "close": 172.5 },
 { "time": 1714521600, "open": 170, "high": 172, "low": 169, "close": 171.8 }]

Account mock:
{ "id": 1, "name": "Paper Account", "balance": 95000, "initial_balance": 100000,
  "positions": [{ "symbol": "AAPL", "quantity": 10, "avg_price": 170, "current_price": 172, "pnl": 20 }] }

Equity mock:
[{ "recorded_at": "2026-04-08T12:00:00", "equity": 95200 }]

Backtest mock result:
{ "sharpe_ratio": 1.2, "max_drawdown": -0.08, "win_rate": 0.62, "profit_factor": 1.8,
  "total_return": 0.15, "equity_curve": [{ "time": 1714521600, "equity": 10500 }] }

Me mock: { "username": "admin" }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth spec + Dashboard tab navigation + WebSocket spec</name>
  <files>
    frontend/e2e/auth.spec.ts,
    frontend/e2e/dashboard.spec.ts
  </files>
  <action>
    All paths: /Users/justinjimenez/Documents/trading-bot/frontend/e2e/

    Create a shared helper: e2e/helpers.ts
    - mockBackend(page): sets up page.route() mocks for all API endpoints used by tests
      - GET /auth/me → { username: 'admin' }
      - GET /api/signals/top* → signals mock array
      - GET /api/market-data/* → candles mock array (DESC order)
      - GET /api/indicators/* → { rsi_14: 55.2, macd: 1.34 }
      - GET /api/paper/accounts/1 → account mock
      - GET /api/paper/accounts/1/equity → equity mock
      - GET /api/paper/accounts/1/compare → { paper_return: 5.2, backtest_return: 7.1 }
      - POST /api/backtest → backtest mock result
    - loginViaUI(page): fills login form with admin/testpassword, submits
      - Also mocks POST /auth/login to return { status: 'ok' } and set cookie header
    - loginViaCookie(page): shortcut — mocks /auth/me directly so app thinks user is authenticated
      - page.route('**/auth/me', route => route.fulfill({ json: { username: 'admin' } }))
      — This avoids needing a real backend for auth cookie

    === e2e/auth.spec.ts ===
    import { test, expect } from '@playwright/test'

    test.describe('Authentication', () => {

      test('landing page is public and shows hero text', async ({ page }) => {
        await page.goto('/')
        await expect(page.getByText(/AI-Powered Trading Intelligence/i)).toBeVisible()
        await expect(page.getByRole('link', { name: /Start Trading|Get Started/i })).toBeVisible()
      })

      test('unauthenticated /dashboard redirects to /login', async ({ page }) => {
        // Mock /auth/me to return 401 (no session)
        await page.route('**/auth/me', route => route.fulfill({ status: 401, json: { detail: 'Not authenticated' } }))
        await page.goto('/dashboard')
        await expect(page).toHaveURL(/\/login/)
      })

      test('login page renders form', async ({ page }) => {
        await page.goto('/login')
        await expect(page.getByLabel(/username/i)).toBeVisible()
        await expect(page.getByLabel(/password/i)).toBeVisible()
        await expect(page.getByRole('button', { name: /sign in/i })).toBeVisible()
      })

      test('login with valid credentials redirects to /dashboard', async ({ page }) => {
        // Mock login endpoint
        await page.route('**/auth/login', route => route.fulfill({ status: 200, json: { status: 'ok' } }))
        await page.route('**/auth/me', route => route.fulfill({ status: 200, json: { username: 'admin' } }))
        // Mock all dashboard data
        await page.route('**/api/**', route => route.fulfill({ status: 200, json: [] }))

        await page.goto('/login')
        await page.getByLabel(/username/i).fill('admin')
        await page.getByLabel(/password/i).fill('testpassword')
        await page.getByRole('button', { name: /sign in/i }).click()
        await expect(page).toHaveURL(/\/dashboard/)
      })

      test('login with wrong credentials shows error message', async ({ page }) => {
        await page.route('**/auth/login', route => route.fulfill({ status: 401, json: { detail: 'Invalid credentials' } }))
        await page.goto('/login')
        await page.getByLabel(/username/i).fill('admin')
        await page.getByLabel(/password/i).fill('wrongpassword')
        await page.getByRole('button', { name: /sign in/i }).click()
        // Error message visible (does not navigate away)
        await expect(page.getByText(/invalid credentials/i)).toBeVisible()
        await expect(page).toHaveURL(/\/login/)
      })

      test('logout redirects to /login', async ({ page }) => {
        const { mockBackend } = await import('./helpers')
        await mockBackend(page)
        await page.route('**/auth/logout', route => route.fulfill({ status: 200, json: { status: 'ok' } }))
        await page.goto('/dashboard')

        await page.getByRole('button', { name: /logout/i }).click()
        await expect(page).toHaveURL(/\/login/)
      })

      test('landing page CTA navigates to /login', async ({ page }) => {
        await page.goto('/')
        await page.getByRole('link', { name: /Start Trading|Get Started/i }).first().click()
        await expect(page).toHaveURL(/\/login/)
      })
    })

    === e2e/dashboard.spec.ts ===
    import { test, expect } from '@playwright/test'

    test.describe('Dashboard — tab navigation', () => {
      test.beforeEach(async ({ page }) => {
        const { mockBackend } = await import('./helpers')
        await mockBackend(page)
        await page.goto('/dashboard')
      })

      test('dashboard nav renders with username', async ({ page }) => {
        await expect(page.getByTestId('dashboard-nav')).toBeVisible()
        await expect(page.getByText('admin')).toBeVisible()
      })

      test('all 4 tabs are present', async ({ page }) => {
        const tabs = page.getByTestId('dashboard-tabs')
        await expect(tabs.getByRole('tab', { name: /chart/i })).toBeVisible()
        await expect(tabs.getByRole('tab', { name: /signals/i })).toBeVisible()
        await expect(tabs.getByRole('tab', { name: /portfolio/i })).toBeVisible()
        await expect(tabs.getByRole('tab', { name: /backtest/i })).toBeVisible()
      })

      test('Signals tab renders signal card', async ({ page }) => {
        await page.getByRole('tab', { name: /signals/i }).click()
        await expect(page.getByTestId('signal-aapl')).toBeVisible({ timeout: 5000 })
      })

      test('Portfolio tab renders portfolio view', async ({ page }) => {
        await page.getByRole('tab', { name: /portfolio/i }).click()
        await expect(page.getByTestId('portfolio-view')).toBeVisible()
        await expect(page.getByTestId('positions-table')).toBeVisible()
      })

      test('Backtest tab renders form and run button', async ({ page }) => {
        await page.getByRole('tab', { name: /backtest/i }).click()
        await expect(page.getByTestId('backtest-results')).toBeVisible()
        await expect(page.getByRole('button', { name: /run backtest/i })).toBeVisible()
      })

      test('no JavaScript errors on any tab', async ({ page }) => {
        const errors: string[] = []
        page.on('console', msg => { if (msg.type() === 'error') errors.push(msg.text()) })

        for (const tab of [/chart/i, /signals/i, /portfolio/i, /backtest/i]) {
          await page.getByRole('tab', { name: tab }).click()
          await page.waitForTimeout(500)
        }
        expect(errors.filter(e => !e.includes('favicon'))).toHaveLength(0)
      })
    })

    test.describe('Dashboard — WebSocket real-time updates', () => {
      test('signal_update via WebSocket adds signal card to feed', async ({ page }) => {
        const { mockBackend } = await import('./helpers')
        await mockBackend(page)

        // Mock WebSocket — send signal_update on connect
        await page.routeWebSocket(/\/ws\/live/, ws => {
          ws.onopen = () => {
            ws.send(JSON.stringify({
              type: 'signal_update',
              signal: {
                symbol: 'TSLA', direction: 'BUY', confidence: 90, regime: 'trending',
                close: 250.0, entry_price: 251.0, stop_loss: 245.0, target_price: 270.0,
                rsi_14: 60, macd_val: 2.1, adx_14: 30, atr_14: 3.5, reasons: ['MACD crossover'],
                interval: '1D', scanned_at: new Date().toISOString()
              }
            }))
          }
        })

        await page.goto('/dashboard')
        await page.getByRole('tab', { name: /signals/i }).click()
        await expect(page.getByTestId('signal-tsla')).toBeVisible({ timeout: 5000 })
      })

      test('price_tick via WebSocket does not crash the page', async ({ page }) => {
        const { mockBackend } = await import('./helpers')
        await mockBackend(page)

        await page.routeWebSocket(/\/ws\/live/, ws => {
          ws.onopen = () => {
            ws.send(JSON.stringify({
              type: 'price_tick',
              symbol: 'AAPL',
              candle: { time: 1714608000, open: 172, high: 173, low: 171, close: 172.5 }
            }))
          }
        })

        await page.goto('/dashboard')
        // No crash — page still shows nav
        await expect(page.getByTestId('dashboard-nav')).toBeVisible()
      })
    })

    test.describe('Mobile responsive — iPhone 14 viewport', () => {
      test.use({ viewport: { width: 390, height: 844 } })

      test('landing page fits mobile viewport without horizontal scroll', async ({ page }) => {
        await page.goto('/')
        const scrollWidth = await page.evaluate(() => document.body.scrollWidth)
        const clientWidth = await page.evaluate(() => document.body.clientWidth)
        expect(scrollWidth).toBeLessThanOrEqual(clientWidth + 2)  // 2px tolerance
      })

      test('dashboard tabs are accessible on mobile', async ({ page }) => {
        const { mockBackend } = await import('./helpers')
        await mockBackend(page)
        await page.goto('/dashboard')
        await expect(page.getByTestId('dashboard-tabs')).toBeVisible()
        // Tabs should be scrollable/visible — at least first tab clickable
        await page.getByRole('tab', { name: /signals/i }).click()
        await expect(page.getByRole('tab', { name: /signals/i })).toHaveAttribute('data-state', 'active')
      })
    })
  </action>
  <verify>
    <automated>cd /Users/justinjimenez/Documents/trading-bot/frontend && npx playwright test e2e/auth.spec.ts e2e/dashboard.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>
    - npx playwright test e2e/auth.spec.ts passes all auth tests
    - npx playwright test e2e/dashboard.spec.ts passes all tab + WebSocket + mobile tests
    - page.routeWebSocket mock pattern used for /ws/live tests
    - No test relies on a live backend (all mocked via page.route / page.routeWebSocket)
  </done>
</task>

<task type="auto">
  <name>Task 2: Signals, Portfolio, Backtest E2E specs</name>
  <files>
    frontend/e2e/signals.spec.ts,
    frontend/e2e/portfolio.spec.ts,
    frontend/e2e/backtest.spec.ts
  </files>
  <action>
    All paths: /Users/justinjimenez/Documents/trading-bot/frontend/e2e/

    === e2e/signals.spec.ts ===
    import { test, expect } from '@playwright/test'

    test.describe('Signal Feed', () => {
      test.beforeEach(async ({ page }) => {
        const { mockBackend } = await import('./helpers')
        await mockBackend(page)
        await page.goto('/dashboard')
        await page.getByRole('tab', { name: /signals/i }).click()
      })

      test('renders AAPL BUY signal card from mock data', async ({ page }) => {
        const card = page.getByTestId('signal-aapl')
        await expect(card).toBeVisible({ timeout: 5000 })
        await expect(card.getByText('BUY')).toBeVisible()
        await expect(card.getByText('85')).toBeVisible()  // confidence
      })

      test('signal card shows entry price, stop-loss, target', async ({ page }) => {
        const card = page.getByTestId('signal-aapl')
        await expect(card).toBeVisible({ timeout: 5000 })
        await expect(card.getByText(/172/)).toBeVisible()   // entry price
        await expect(card.getByText(/168/)).toBeVisible()   // stop-loss
        await expect(card.getByText(/180/)).toBeVisible()   // target
      })

      test('signal card shows regime badge', async ({ page }) => {
        const card = page.getByTestId('signal-aapl')
        await expect(card).toBeVisible({ timeout: 5000 })
        await expect(card.getByText(/trending/i)).toBeVisible()
      })

      test('signal feed updates without page refresh on WebSocket push', async ({ page }) => {
        // Already have AAPL in mock — push MSFT via WS
        await page.routeWebSocket(/\/ws\/live/, ws => {
          ws.onopen = () => {
            ws.send(JSON.stringify({
              type: 'signal_update',
              signal: {
                symbol: 'MSFT', direction: 'SELL', confidence: 72, regime: 'ranging',
                close: 420.0, entry_price: 419.0, stop_loss: 425.0, target_price: 410.0,
                rsi_14: 68, macd_val: -0.5, adx_14: 15, atr_14: 4.1, reasons: ['RSI overbought'],
                interval: '1D', scanned_at: new Date().toISOString()
              }
            }))
          }
        })
        // Navigate to signals tab (WebSocket mock applied before goto)
        await page.goto('/dashboard')
        await page.getByRole('tab', { name: /signals/i }).click()
        await expect(page.getByTestId('signal-msft')).toBeVisible({ timeout: 5000 })
      })

      test('SELL signal badge has different styling than BUY', async ({ page }) => {
        // Override mock to return SELL signal
        await page.route('**/api/signals/top*', route => route.fulfill({
          status: 200,
          json: [{ symbol: 'MSFT', direction: 'SELL', confidence: 70, regime: 'ranging',
                   close: 420, entry_price: 419, stop_loss: 425, target_price: 410,
                   rsi_14: 68, macd_val: -0.5, adx_14: 15, atr_14: 4.1, reasons: [],
                   interval: '1D', scanned_at: new Date().toISOString() }]
        }))
        await page.reload()
        await page.getByRole('tab', { name: /signals/i }).click()
        const sellBadge = page.getByTestId('signal-msft').getByText('SELL')
        await expect(sellBadge).toBeVisible({ timeout: 5000 })
      })
    })

    === e2e/portfolio.spec.ts ===
    test.describe('Portfolio View', () => {
      test.beforeEach(async ({ page }) => {
        const { mockBackend } = await import('./helpers')
        await mockBackend(page)
        await page.goto('/dashboard')
        await page.getByRole('tab', { name: /portfolio/i }).click()
      })

      test('portfolio view renders without crash', async ({ page }) => {
        await expect(page.getByTestId('portfolio-view')).toBeVisible({ timeout: 5000 })
      })

      test('shows account balance from mock', async ({ page }) => {
        await expect(page.getByTestId('portfolio-view')).toBeVisible({ timeout: 5000 })
        await expect(page.getByText(/95,000|95000/)).toBeVisible()
      })

      test('positions table shows AAPL row', async ({ page }) => {
        const table = page.getByTestId('positions-table')
        await expect(table).toBeVisible({ timeout: 5000 })
        await expect(table.getByText('AAPL')).toBeVisible()
        await expect(table.getByText('10')).toBeVisible()  // quantity
      })

      test('equity curve chart renders', async ({ page }) => {
        await expect(page.getByTestId('equity-curve-chart')).toBeVisible({ timeout: 5000 })
      })

      test('allocation pie chart renders', async ({ page }) => {
        await expect(page.getByTestId('allocation-pie-chart')).toBeVisible({ timeout: 5000 })
      })

      test('P&L shows negative value when balance < initial', async ({ page }) => {
        // Mock returns balance=95000 < initial=100000 → P&L = -5000
        await expect(page.getByTestId('portfolio-view')).toBeVisible({ timeout: 5000 })
        await expect(page.getByText(/-5,000|-5000/)).toBeVisible()
      })
    })

    === e2e/backtest.spec.ts ===
    test.describe('Backtest Results', () => {
      test.beforeEach(async ({ page }) => {
        const { mockBackend } = await import('./helpers')
        await mockBackend(page)
        await page.goto('/dashboard')
        await page.getByRole('tab', { name: /backtest/i }).click()
      })

      test('backtest form renders with default symbol AAPL', async ({ page }) => {
        await expect(page.getByTestId('backtest-results')).toBeVisible({ timeout: 5000 })
        await expect(page.getByRole('button', { name: /run backtest/i })).toBeVisible()
      })

      test('clicking Run Backtest calls POST /api/backtest', async ({ page }) => {
        let backtestCalled = false
        await page.route('**/api/backtest', async route => {
          backtestCalled = true
          await route.fulfill({ status: 200, json: {
            sharpe_ratio: 1.2, max_drawdown: -0.08, win_rate: 0.62,
            profit_factor: 1.8, total_return: 0.15,
            equity_curve: [{ time: 1714521600, equity: 10500 }]
          }})
        })

        await page.getByRole('button', { name: /run backtest/i }).click()
        await expect(page.getByTestId('backtest-metrics')).toBeVisible({ timeout: 8000 })
        expect(backtestCalled).toBe(true)
      })

      test('backtest metrics display Sharpe ratio', async ({ page }) => {
        await page.getByRole('button', { name: /run backtest/i }).click()
        const metrics = page.getByTestId('backtest-metrics')
        await expect(metrics).toBeVisible({ timeout: 8000 })
        await expect(metrics.getByText(/1.2|sharpe/i)).toBeVisible()
      })

      test('backtest equity chart renders after run', async ({ page }) => {
        await page.getByRole('button', { name: /run backtest/i }).click()
        await expect(page.getByTestId('backtest-equity-chart')).toBeVisible({ timeout: 8000 })
      })

      test('changing symbol input updates the backtest target', async ({ page }) => {
        // Find symbol input and change to MSFT
        const symbolInput = page.getByPlaceholder(/symbol/i).or(page.locator('input[value="AAPL"]'))
        await symbolInput.clear()
        await symbolInput.fill('MSFT')

        let requestBody: Record<string, unknown> = {}
        await page.route('**/api/backtest', async route => {
          requestBody = JSON.parse(route.request().postData() ?? '{}') as Record<string, unknown>
          await route.fulfill({ status: 200, json: {
            sharpe_ratio: 0.9, max_drawdown: -0.1, win_rate: 0.55,
            profit_factor: 1.4, total_return: 0.08,
            equity_curve: [{ time: 1714521600, equity: 10200 }]
          }})
        })

        await page.getByRole('button', { name: /run backtest/i }).click()
        await expect(page.getByTestId('backtest-metrics')).toBeVisible({ timeout: 8000 })
        expect(requestBody['symbol']).toBe('MSFT')
      })
    })

    Run full suite check:
    cd /Users/justinjimenez/Documents/trading-bot/frontend && npx playwright test --reporter=list 2>&1 | tail -30
  </action>
  <verify>
    <automated>cd /Users/justinjimenez/Documents/trading-bot/frontend && npx playwright test --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All 5 spec files exist: auth.spec.ts, dashboard.spec.ts, signals.spec.ts, portfolio.spec.ts, backtest.spec.ts
    - npx playwright test completes without runner/config errors
    - Tests that can pass without live backend (all mocked) pass
    - WebSocket tests use page.routeWebSocket() (not live WS connection)
    - Mobile viewport tests included (390x844)
    - Test count: at minimum 25 tests across all 5 specs
  </done>
</task>

</tasks>

<verification>
cd /Users/justinjimenez/Documents/trading-bot/frontend && npx playwright test --reporter=list 2>&1 | grep -E "passed|failed|error" | tail -5
Expected: tests listed, no runner errors. Some may fail on missing UI elements until frontend is fully built — that is acceptable. The runner must not crash.
</verification>

<success_criteria>
- 5 Playwright spec files cover: auth flows, tab navigation, WebSocket mocking, chart render, portfolio display, backtest execution, mobile viewport
- All tests mock backend via page.route() and page.routeWebSocket() — no live backend required
- data-testid selectors match what Plan 03 components implement
- npx playwright test runner exits cleanly (no config errors)
- At minimum 25 distinct test cases across all specs
- Mobile responsive test verifies no horizontal scroll at 390px width
</success_criteria>

<output>
After completion, create .planning/phases/05-dashboard/05-04-SUMMARY.md
</output>
